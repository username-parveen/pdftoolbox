{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the primary PDF Toolbox window",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
